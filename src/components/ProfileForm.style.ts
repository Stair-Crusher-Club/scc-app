import styled from 'styled-components/native';

import {font} from '@/constant/font';

export const Gap = styled.View({height: 30});
export const Label = styled.Text({
  fontSize: 16,
  lineHeight: '24px',
  fontFamily: font.pretendardBold,
  marginBottom: 8,
});
