import {MainNavigationScreens} from './Navigation.screens';

// Native: no web-only screens. (See webScreens.web.tsx for the web build.)
export const webOnlyScreens: typeof MainNavigationScreens = [];
