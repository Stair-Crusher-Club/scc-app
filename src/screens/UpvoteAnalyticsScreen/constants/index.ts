import {TabType} from '../types';

export const tabItems = [
  {value: 'users' as TabType, label: '사용자'},
  {value: 'stats' as TabType, label: '사용자 통계'},
];

export const INITIAL_TAB: TabType = 'users';
